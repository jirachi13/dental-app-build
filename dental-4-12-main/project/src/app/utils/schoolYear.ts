// The school-year rules live in `shared/` so the server uses the SAME ones
// (Sprint 140) — re-exported here so every existing import keeps working.
export { schoolYearStart, schoolYearEnd, schoolYearLabel } from '../../../shared/schoolYear';
